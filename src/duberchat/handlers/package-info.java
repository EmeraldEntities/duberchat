/**
 * Contains all the handlers for the various events used in both client and
 * server side of this application.
 * <p>
 * These handlers are further sorted into client and server handlers, but common
 * classes/interfaces shared by both (eg. the
 * {@link duberchat.handlers.Handleable Handleable class}) will be in this top
 * level package.
 */
package duberchat.handlers;
